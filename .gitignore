# Entorno — nunca commitear valores reales
.env
.env.local
.env.*.local
!.env.example

# Dependencias
node_modules/
.venv/
vendor/

# Build estático de Vite (se genera en CI, no se commitea)
dist/

# Caché de Vite
.vite/

# Info de build incremental de TypeScript
*.tsbuildinfo

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Worktrees temporales de subagentes de Claude Code (aislamiento de sesión, no versionar)
.claude/worktrees/
